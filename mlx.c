#include "minirt.h"

void mlx_start(t_options *data)
{
	//initialise mlx_ptr 
	//start mlx_window with the correct format (i think I can just choose one)
	//load the map // call ray tracing launch_rays(data);
}